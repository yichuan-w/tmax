[project]
name = "tmax"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "harbor",
    "litellm",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
